"main";let express;_523‍.w('express',[["default",function(v){express=v}]]);let cors;_523‍.w('cors',[["default",function(v){cors=v}]]);let CreateCarsService;_523‍.w('../cars/services/CreateCarsService.js',[["default",function(v){CreateCarsService=v}]]);let ListCarsService;_523‍.w('../cars/services/ListCarsService.js',[["default",function(v){ListCarsService=v}]]);





const app = express();

app.use(express.json());
app.use(cors());

// const repositoriesCars = [];

app.get('/list', ListCarsService.execute);
app.post('/create', CreateCarsService.execute);

/*app.put('/update/:id', async (req, res) => {
  const { id, brand, model, year, gas_type, price, color } = req.body;

  const checkIdCar = repositoriesCars.find({where: 'id' == id});

  if(!checkIdCar) {
    return res.status(400).json({ error: 'id not found'});
  }

  const updatingCar = repositoriesCars.filter('id').push(brand, model, year, gas_type, price, color)

  return res.status(200).json(updatingCar)
}); */

app.delete('/delete/:id', async (req, res) => {

});

_523‍.d(app);